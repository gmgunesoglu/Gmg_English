import { MinContextLengthDirective } from './min-context-length.directive';

describe('MinContextLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinContextLengthDirective();
    expect(directive).toBeTruthy();
  });
});
